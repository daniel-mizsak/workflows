# SETUP JUST

Setup rust-just with uv

```yaml
uses: daniel-mizsak/workflows/.github/actions/setup-just@v1
with:
  # Just version to install
  just-version: "1.43.0"

  # Python version installed by uv
  python-version: "3.12"
```
